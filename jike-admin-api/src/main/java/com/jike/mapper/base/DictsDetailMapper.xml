<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.jike.mapper.base.DictsDetailMapper">

    <sql id="selectComps">
        select
            code as code,
            enable as enable,
            extra as extra,
            id as id,
            name as name,
            order_index as orderIndex,
            type_code as typeCode
        from tb_dicts_detail
    </sql>

</mapper>