<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.jike.mapper.ae.AEMaterialMapper">

    <sql id="selectComps">
        select
            cover_url as coverUrl,
            enabled as enabled,
            id as id,
            name as name,
            order_index as orderIndex,
            right as right,
            type_id as typeId,
            url as url
        from tb_ae_plugin_materials
    </sql>

</mapper>