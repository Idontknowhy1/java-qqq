<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.jike.mapper.chat.ConversationsMapper">

    <!--<sql id="selectComps">
        select
            create_time,
            icon,
            id,
            instruction,
            title,
            update_time,
            user_id
        from tb_script_genre_convs
    </sql>
 -->
</mapper>